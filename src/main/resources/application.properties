spring.application.name=shortlink
# Used when no profile is set (./mvnw spring-boot:run). Lambda sets SPRING_PROFILES_ACTIVE=lambda.
spring.profiles.default=local
app.dynamodb.table-name=${TABLE_NAME:}
app.sqs.queue-url=${CLICK_QUEUE_URL:}
